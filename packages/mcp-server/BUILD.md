# Inky MCP Server Package

This package implements the MCP (Model Context Protocol) server for Inky.

## Building

```bash
cd packages/mcp-server
pnpm install
pnpm build
```

## Publishing

```bash
cd packages/mcp-server
npm publish --access public
```

Note: Make sure to update the version in `package.json` before publishing.

